package com.spotride.mobile.ui.screen.loginscreen

import com.spotride.mobile.model.user.api.UserApiService

class LoginScreenController(userApiService: UserApiService) {

    fun doLoginUser(email: String, password: String) {
        // TODO: login user: userApiService.loginUser(email, password).getOrThrow()
    }
}